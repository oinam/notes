{
  "name": "oinam-notes",
  "description": "Notes by Oinam — built with Ovellum.",
  "private": true,
  "scripts": {
    "dev": "ovellum dev",
    "build": "ovellum build && npm run postbuild",
    "postbuild": "rm -f dist/package.json dist/package-lock.json dist/ovellum.config.json dist/.gitignore && find dist -name .DS_Store -delete",
    "check": "ovellum check"
  },
  "devDependencies": {
    "ovellum": "^0.4.0"
  }
}
